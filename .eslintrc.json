{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "@typescript-eslint/no-unused-vars": [
      "warn",
      {
        "args": "none", // Ignore unused function parameters
        "argsIgnorePattern": "^_" // Optionally, ignore variables that start with "_"
      }
    ]
  }
}
